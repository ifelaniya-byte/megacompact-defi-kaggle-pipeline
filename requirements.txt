# Megacompact DeFi Kaggle pipeline dependencies.
#
# Core (required) — Kaggle notebooks ship these pre-installed.
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
pydantic>=2.0          # real MegaCompact16 data contracts

# Optional — only needed if you use the corresponding optional paths in
# megacompact_core/core.py. The engine degrades gracefully without them.
#
# pyyaml>=6.0          # Config.from_yaml / to_yaml and the Typer CLI
# pyarrow>=14.0        # Parquet I/O (ArtifactStore.save_parquet)
# torch>=2.0           # world-model / bootstrap ensemble training
# fastapi>=0.100       # read-only API server
# uvicorn>=0.23        # API server
# typer>=0.9           # CLI
# rich>=13.0           # console output
# matplotlib>=3.7      # charts
# scipy>=1.10          # statistical features
# faiss-cpu>=1.7       # retrieval memory (falls back to sklearn)
# networkx>=3.0        # graph features
